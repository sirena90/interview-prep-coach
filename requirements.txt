# Interview Prep Coach — simplified Streamlit version
# Python 3.10+ recommended

# UI
streamlit>=1.28.0

# LLM providers (multi-provider — selected via LLM_PROVIDER in .env)
anthropic>=0.39.0
openai>=1.0.0          # also used for Mistral via its OpenAI-compatible API

# Observability — tracing for every LLM call (no-op without LANGSMITH_API_KEY)
langsmith>=0.8.0

# Vector store (RAG)
chromadb>=0.5.0

# Data validation + settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CV parsing (PDF text extraction)
pypdf>=4.0.0

# Environment variable loading
python-dotenv>=1.0.0

protobuf==3.20.*
