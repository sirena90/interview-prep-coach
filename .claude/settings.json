{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q '\\.py$' && uv run ruff check --fix --quiet \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Linting with ruff..."
          }
        ]
      }
    ]
  }
}
